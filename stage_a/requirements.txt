# Stage A (SAR detection) dependencies.
#
# Deliberately NOT added to backend/pyproject.toml: the FastAPI service must never
# import torch. The backend reads Stage A's output artifact through the frozen
# DetectionResult contract, so the API stays light and deployable on its own.
#
# Install into a separate environment:
#   python -m venv stage_a/.venv
#   stage_a/.venv/Scripts/activate      # Windows
#   source stage_a/.venv/bin/activate   # macOS / Linux
#   pip install -r stage_a/requirements.txt

# --- Model ---------------------------------------------------------------
# On Apple Silicon (M-series) and CUDA machines the default wheel is correct.
# For a CPU-only Linux box, install torch from the CPU index first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.4
segmentation-models-pytorch>=0.3.4

# --- Imagery / geospatial ------------------------------------------------
rasterio>=1.3
pillow>=10.0
numpy>=1.26
scipy>=1.11          # median despeckle filter
shapely>=2.0
pyproj>=3.7

# --- Tooling -------------------------------------------------------------
pytest>=8.3
ruff>=0.7
